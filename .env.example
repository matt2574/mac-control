# mac-control service configuration

# Port to listen on (default: 9999)
PORT=9999

# Shared secret for authenticated endpoints (execute, settings update).
# Must match the MAC_CONTROL_SECRET in your tru-tech_os .env.local
MAC_CONTROL_SECRET=dev-secret-change-me
